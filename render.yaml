services:
  - type: web
    name: portfolio
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    plan: free
    env: production
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /