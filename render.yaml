services:
  - type: web
    name: portfolio
    runtime: node
    buildCommand: npm ci && cd frontend && npm ci && cd .. && npm run build
    startCommand: npm start
    plan: free
    env: production
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /